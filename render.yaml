services:
  - type: web
    name: finance-parser
    runtime: python
    plan: free
    buildCommand: uv sync
    startCommand: uv run gunicorn app:app
    envVars:
      - key: API_KEY
        sync: false
      - key: STATEMENT_PASSWORD
        sync: false


